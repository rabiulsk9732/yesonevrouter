#!/bin/bash
#
# Stop YESRouter daemon (like 'service vpp stop')
#

pkill -9 yesrouter 2>/dev/null

if [ $? -eq 0 ]; then
    echo "✓ YESRouter daemon stopped"
else
    echo "No yesrouter process found"
fi
